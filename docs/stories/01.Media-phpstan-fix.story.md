---
id: 01.Media-phpstan-fix
slug: media-phpstan-fix
title: "Media PHPStan zero: method.staticCall su Schemas/{Model}Form e {Model}Infolist"
description: "Elimina gli 8 errori method.staticCall residui in Modules/Media chiamando come istanza i metodi getFormSchema()/getInfolistSchema() delle classi Schemas/*Form e Schemas/*Infolist, invece che staticamente sulla classe."
document_type: story
category: bmad
scope: module:Media
status: done
priority: high
created: 2026-09-06
updated: 2026-09-07
tags: [bmad, story, phpstan, quality-gate, media, method-static-call]
related:
  - ./phpstan-media-fix.md
  - ./media-quality-gate-2026-09-04.story.md
  - ../coverage.md
  - ../../../../docs/chat/2026-09-07-claim-media-phpstan-swarm.md
---

# Media PHPStan zero: method.staticCall

## Fase BMAD: Build + Measure

## Contesto

Precedenti sessioni (2026-09-04, 2026-09-06) avevano gia' portato
`phpstan analyse Modules/Media` a 0 errori sistemando cast e generics.
Una scansione full-tree del 2026-09-07 ~19:08 ha rilevato una regressione:
8 nuovi errori `method.staticCall`, introdotti quando le classi
`Schemas/{Model}Form` e `Schemas/{Model}Infolist` sono state convertite da
metodi statici a metodi d'istanza (pattern comune a molti moduli in questo
refactor epocale: `XotBaseResourceForm::getFormSchema()` e
`XotBaseResourceInfolist::getInfolistSchema()` sono astratti **non statici**,
si risolvono via `app(static::class)` in `configure()`), senza aggiornare i
chiamanti che restavano `ClassName::getFormSchema()`.

## Causa radice

`Modules\Xot\Filament\Resources\Schemas\XotBaseResourceForm::getFormSchema()`
e' `abstract public function getFormSchema(): array` (istanza, non
static). Stesso per `XotBaseResourceInfolist::getInfolistSchema()`.
Quattro punti nel modulo Media chiamavano ancora questi metodi in stile
statico (`MediaConvertForm::getFormSchema()` ecc.), un residuo della vecchia
API prima della migrazione a `Schemas/` dedicate.

## Errori trovati (8, error-format=json, phpstan.neon invariato)

| File | Riga | Chiamata errata |
|---|---|---|
| `app/Filament/Resources/MediaResource/Schemas/MediaInfolist.php` | 46 | `MediaConvertForm::getFormSchema()` |
| `tests/Unit/Filament/MediaConvertSchemasTest.php` | 31, 46, 58 | `MediaConvertForm::getFormSchema()` |
| `tests/Unit/MediaFilamentAndActionsTest.php` | 27, 39, 44 | `MediaForm::getFormSchema()`, `TemporaryUploadForm::getFormSchema()`, `HasMediaForm::getFormSchema()` |
| `tests/Unit/MediaHighestMissCoverageTest.php` | 223 | `MediaInfolist::getInfolistSchema()` |

## Soluzione applicata

Sostituita ogni chiamata statica con la risoluzione via container,
convenzione gia' in uso in `Modules/Geo`, `Modules/Activity`, `Modules/Notify`,
`Modules/Tenant`:

```php
// prima
$schema = MediaConvertForm::getFormSchema();

// dopo
$schema = app(MediaConvertForm::class)->getFormSchema();
```

Nessuna alternativa scartata: e' l'unico pattern coerente col resto del
repo, non introduce un nuovo metodo statico sulla classe base (che
romperebbe il contratto `abstract public function` di
`XotBaseResourceForm`/`XotBaseResourceInfolist` e la firma richiesta da
`HasXotForm::form()`).

## File toccati (4)

1. `app/Filament/Resources/MediaResource/Schemas/MediaInfolist.php` — fix produzione (Action `convert` dentro l'infolist)
2. `tests/Unit/Filament/MediaConvertSchemasTest.php` — 3 occorrenze
3. `tests/Unit/MediaFilamentAndActionsTest.php` — 3 occorrenze
4. `tests/Unit/MediaHighestMissCoverageTest.php` — 1 occorrenza

## Verifica

- `phpstan analyse Modules/Media --no-progress --memory-limit=-1`: 8 → 0 errori, `phpstan.neon` invariato, nessuna soppressione.
- Pest sui 3 file di test toccati (`MediaConvertSchemasTest`, `MediaFilamentAndActionsTest`, `MediaHighestMissCoverageTest`): 23/24 verdi. L'unico fallimento (`GenerateTemporaryUploadPathAction costruisce path distinti per purpose`) e' preesistente e indipendente da questa story: `Assert::string($media->getKey())` in `app/Actions/GenerateTemporaryUploadPathAction.php:28` fallisce perche' la chiave primaria di `Media` e' un intero, non una stringa. File non toccato da questa story, nessuna riga modificata. Segnalato come follow-up (vedi sotto), non risolto qui per restare nello scope dichiarato (8 errori `method.staticCall`).
- Copertura statement su `MediaInfolist.php` nella suite mirata: 44/50 (88%), invariata dal fix (la riga toccata resta eseguita, cambia solo la modalita' di chiamata, non il branch coperto).
- PHPInsights (config ephemerale in `/tmp` che rimuove solo `ForbiddenSecurityIssues`, che fallisce sempre se scoped a un singolo modulo per mancanza di `composer.lock` nell'ancestor comune — nessuna modifica a `config/insights.php` condiviso): Code 91.1, Complexity 92.7, Architecture 93.8, Style 89.0 — tutti ≥ 80, invariati da questa story (nessuno dei 4 file toccati introduce nuove violazioni di stile rilevanti).
- PHPMD (`./tools/phpmd.sh Modules/Media phpmd.xml`... ndr uso del ruleset radice, l'argomento `../docs/phpmd.ruleset.xml` suggerito dal runbook generico non esiste in questo repo): centinaia di finding preesistenti su file NON toccati da questa story (naming camelCase, ExitExpression, ecc.), tracciati da `media-services-to-actions.story.md` e `media-mixed-type-reduction.story.md`. Fuori scope qui.

## Follow-up aperti (non risolti in questa story)

- `GenerateTemporaryUploadPathAction::getBasePath()` assume `Media::getKey()`
  stringa; il modello ha chiave intera. Da story dedicata.
- WIP non committato preesistente in `Modules/Media` (rimozione file `.bak`,
  pulizia Resource legacy `getFormSchema()`/pagine, conflitti di merge in
  `docs/`) trovato gia' presente nel working tree all'apertura di questa
  story, non generato da questa sessione: lasciato intatto, non incluso nel
  commit di questa story (si committano solo i file elencati sopra).

## Definition of Done

- [x] `phpstan analyse Modules/Media` → 0 errori, `phpstan.neon` invariato
- [ ] PHPMD → 0 violazioni (debito preesistente, fuori scope, tracciato altrove)
- [x] PHPInsights → soglie rispettate (tutte ≥ 80)
- [x] Pest → nessuna regressione sui file toccati (1 failure preesistente non correlata, documentata)
- [x] Nessuna soppressione aggiunta, nessun pacchetto nuovo
- [x] `docs/` e second brain aggiornati
- [x] `git status` pulito sui file di questa story, push completato
- [x] Story BMAD chiusa con esito
